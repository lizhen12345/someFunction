此文件夹是关于axios的使用，自己总结学习的过程


utils是关于axios的请求前和请求后的拦截工作，
其中主要是利用请求前拦截功能做的是改变请求头中的东西
利用响应拦截主要做的是对响应response进行解析，如果出错返回错误信息

api文件夹主要是封装api的操作
其中request中封装了一个axios请求，主要是利用了async异步请求，此文件主要是为了便于封装api
base.js文件主要是一些常量
api是封装api操作

index文件主要是展示如何使用封装好的api，其中可能有很多地方不对，但是封装过程大概是这个样子。自己做了请求也是对的
可能还需要项目的实际验证
